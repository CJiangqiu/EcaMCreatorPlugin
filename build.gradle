plugins {
    id "java"
}

// No Java compilation needed - this plugin only contains resources
sourceSets {
    main {
        java.srcDirs = []
    }
}

tasks.jar {
    archiveFileName.set("eca_plugin_1.0.7.zip")
    from(sourceSets.main.output)
}
