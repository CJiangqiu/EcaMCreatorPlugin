Mark this explicit entity as the wave's leader instead of an ordinary spawn entry; count is ignored because a leader is always a single entity. Faction-group rows cannot be leaders. A wave has at most one leader — if several explicit rows are marked, the last one wins. Requires a Default Raider Faction; without it the entity is spawned as an ordinary raider.
