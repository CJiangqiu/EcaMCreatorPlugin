Note! This condition is evaluated on the server. Return true to end the raid as a defeat. Leave empty to use ECA's default rule: the target structure no longer covers the raid centre. Note this means an unanchored raid (Structure Anchor set to None) never loses by default, because it has no structure to check — give it a custom defeat condition or a Max Duration. Position dependencies are the raid centre.
