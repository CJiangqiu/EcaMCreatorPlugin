The default faction for explicit spawn rows and wave leaders, picked from the ECA Faction elements in your workspace. A row with its own Group Faction uses that faction instead. Leave as (None) to keep explicit entries unbound; wave leaders then spawn as ordinary raiders.
