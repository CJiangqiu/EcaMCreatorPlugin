Tick the checkbox to enable a frame texture, then select a screen texture to use as the boss bar frame. The frame size is automatically determined from the texture dimensions. Untick the checkbox to disable the texture without clearing your selection.

Can be combined with a shader - the shader will overlay on top of the texture with semi-transparency.