Enable custom boss bar rendering for this entity type. When enabled, the vanilla boss bar is replaced by ECA's custom rendering system.

If enabled but no texture or shader is configured, the vanilla boss bar will be hidden.