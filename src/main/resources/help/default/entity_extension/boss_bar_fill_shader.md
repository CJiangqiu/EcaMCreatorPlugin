Enable a shader effect for the boss bar fill. When enabled, select a shader preset and specify the render width and height.

If a fill texture is also set, the shader will be blended on top of the texture. If no texture is set, only the shader effect will be rendered.