Tick the checkbox to enable a fill texture, then select a screen texture to use as the boss bar fill (health bar). The fill width scales with the entity's health percentage. Untick the checkbox to disable the texture without clearing your selection.

The fill is automatically centered within the frame. Can be combined with a shader.