Select a screen texture to use as the boss bar fill (health bar). The fill width scales with the entity's health percentage.

The fill is automatically centered within the frame. Can be combined with a shader.