Note! The dependencies used by this condition must be available on the client side, otherwise the condition will not work. Optional logic procedure (with x/y/z/world/entity dependencies) that decides whether this render-layer entry is used. Entries are checked top to bottom; the first one whose condition returns true is rendered. Leave empty to make this entry the unconditional default (it applies when no conditional entry above it matches).
