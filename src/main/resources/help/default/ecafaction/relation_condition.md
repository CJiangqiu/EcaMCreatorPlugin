Note! This condition is evaluated on the server. Leave it empty to make this row a static preset. When set, the relation applies only while the condition returns true, otherwise resolution falls back to the static presets. The `entity` dependency is the target being judged, and `sourceentity` is a member of this faction — it may be empty when the query has no member context, so do not rely on it unconditionally.
