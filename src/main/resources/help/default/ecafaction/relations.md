How this faction treats other factions and entities with no faction. Each row targets one faction with one relation, and may carry a condition. Rows without a condition become static presets; rows with one are evaluated at runtime and take priority, falling back to the presets when false. Use a **(No faction)** row to set how this faction treats entities that belong to no faction at all.
