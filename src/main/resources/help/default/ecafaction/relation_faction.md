The faction this row applies to. Only ECA Faction elements registered in your workspace are listed, minus this faction itself — a faction's relation to itself is derived by ECA, not configured. Pick **(No faction)** to target entities that belong to no faction at all: such a row without a condition becomes this faction's default relation, and with a condition it is evaluated at runtime and falls back to that default when false.
