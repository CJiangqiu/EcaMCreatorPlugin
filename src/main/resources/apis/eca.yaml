name: Epic Core API

forge-1.20.1:
  required_when_enabled: true
  gradle: |
    repositories {
      flatDir { dirs new File(System.getProperty("user.home"), ".mcreator/lib") }
    }
    dependencies {
      implementation name: 'eca-1.20.1-forge-1.0.6-dev'
    }
